using System;

namespace Library
{
    /// <summary>
    /// Represents the potential types of which a user can be.
    /// </summary>
    public enum UserType
    {
        Admin, Entrepeneur, Company
    }
}
